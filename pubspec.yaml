name: bulecuentas
description: App para seleccionar y procesar imágenes
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  google_mlkit_text_recognition: ^0.11.0  # ESTA ES LA QUE NECESITAS

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
